t = int(input())
for _ in range(t):
    n, x, y, z = map(int, input().split())
    no = (n + (x + y) - 1) // (x + y)

    if x * z >= n:
        ai = (n + x - 1) // x
    else:
        rem = n - x * z
        ai = z + (rem + (x + 10 * y) - 1) // (x + 10 * y)
    print(min(no, ai))